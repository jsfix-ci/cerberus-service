/* eslint-disable */
import { render, screen } from '@testing-library/react';
import React from 'react';

import TaskListPage from './TaskListPage';

describe('TaskListCard', () => {
  it('should act as a placeholder', () => {
    expect(true).toBeTruthy();
  });
});
